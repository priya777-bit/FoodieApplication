import { ListenerDirective } from './listener.directive';

describe('ListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenerDirective();
    expect(directive).toBeTruthy();
  });
});
